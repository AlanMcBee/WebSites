<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
@await Html.PartialAsync(Url.Content("~/Views/Partials/head/meta/default.cshtml"))
@await Html.PartialAsync(Url.Content("~/Views/Partials/head/site/icon.cshtml"))
@{
    var env = Context.RequestServices.GetService<IWebHostEnvironment?>();
    if (env?.IsDevelopment() ?? false)
    {
        <!-- Font Awesome icons (free version)-->
    }
}
@await Html.PartialAsync(Url.Content("~/Views/Partials/head/fonts/fontAwesome.cshtml"))
@{
    if (env?.IsDevelopment() ?? false)
    {
        <!-- Google fonts-->
    }
}
@await Html.PartialAsync(Url.Content("~/Views/Partials/head/fonts/google/Montserrat.cshtml"))
@await Html.PartialAsync(Url.Content("~/Views/Partials/head/fonts/google/Lato.cshtml"))
@{
    if (env?.IsDevelopment() ?? false)
    {
        <!-- Core theme CSS (includes Bootstrap)-->
    }
}
<link href="@Url.Content("~/Bootstrap/Freelancer/css/styles.css")" rel="stylesheet" asp-append-version="true" />
