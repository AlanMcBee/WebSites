@{
    var env = Context.RequestServices.GetService<IWebHostEnvironment?>();
    if (env?.IsDevelopment() ?? false)
    {
        <!-- Favicon-->
    }
}
<link rel="icon" type="image/x-icon" href="Bootstrap/Freelancer/assets/favicon.ico" asp-append-version="true" />
